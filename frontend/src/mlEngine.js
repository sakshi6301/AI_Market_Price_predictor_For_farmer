export const crops = {
  Wheat: { base: 2425, volatility: 0.08, demand: 0.73, season: "Rabi", soil: "Alluvial soil", waterNeed: "Medium" },
  Rice: { base: 3100, volatility: 0.1, demand: 0.78, season: "Kharif", soil: "Clay soil", waterNeed: "High" },
  "Corn/Maize": { base: 2250, volatility: 0.12, demand: 0.7, season: "Kharif", soil: "Alluvial soil", waterNeed: "Medium" },
  Soybean: { base: 4550, volatility: 0.13, demand: 0.66, season: "Kharif", soil: "Black soil", waterNeed: "Medium" },
  Cotton: { base: 6500, volatility: 0.11, demand: 0.69, season: "Kharif", soil: "Black soil", waterNeed: "Medium" },
  Tomato: { base: 2200, volatility: 0.19, demand: 0.76, season: "Zaid", soil: "Red soil", waterNeed: "Medium" },
  Onion: { base: 1850, volatility: 0.24, demand: 0.82, season: "Rabi", soil: "Black soil", waterNeed: "Low" },
  Potato: { base: 1600, volatility: 0.17, demand: 0.71, season: "Rabi", soil: "Sandy soil", waterNeed: "Medium" },
  Sugarcane: { base: 3550, volatility: 0.07, demand: 0.74, season: "Kharif", soil: "Black soil", waterNeed: "High" },
  Mustard: { base: 5450, volatility: 0.1, demand: 0.73, season: "Rabi", soil: "Alluvial soil", waterNeed: "Low" },
  Groundnut: { base: 6800, volatility: 0.14, demand: 0.68, season: "Kharif", soil: "Sandy soil", waterNeed: "Low" },
  Sunflower: { base: 6200, volatility: 0.13, demand: 0.64, season: "Rabi", soil: "Red soil", waterNeed: "Low" },
  Jowar: { base: 3200, volatility: 0.1, demand: 0.63, season: "Kharif", soil: "Black soil", waterNeed: "Low" },
  Bajra: { base: 2800, volatility: 0.11, demand: 0.62, season: "Kharif", soil: "Sandy soil", waterNeed: "Low" },
  Ragi: { base: 3900, volatility: 0.09, demand: 0.65, season: "Kharif", soil: "Red soil", waterNeed: "Low" },
  "Arhar/Tur Dal": { base: 7800, volatility: 0.14, demand: 0.77, season: "Kharif", soil: "Black soil", waterNeed: "Low" },
  "Moong Dal": { base: 8200, volatility: 0.13, demand: 0.74, season: "Zaid", soil: "Sandy soil", waterNeed: "Low" },
  "Urad Dal": { base: 7600, volatility: 0.13, demand: 0.72, season: "Kharif", soil: "Black soil", waterNeed: "Low" },
  "Chana/Gram": { base: 5600, volatility: 0.12, demand: 0.76, season: "Rabi", soil: "Black soil", waterNeed: "Low" },
  "Masoor Dal": { base: 6400, volatility: 0.11, demand: 0.7, season: "Rabi", soil: "Alluvial soil", waterNeed: "Low" },
  Garlic: { base: 9200, volatility: 0.25, demand: 0.81, season: "Rabi", soil: "Alluvial soil", waterNeed: "Low" },
  Ginger: { base: 8200, volatility: 0.22, demand: 0.78, season: "Kharif", soil: "Red soil", waterNeed: "Medium" },
  Turmeric: { base: 12800, volatility: 0.2, demand: 0.8, season: "Kharif", soil: "Red soil", waterNeed: "Medium" },
  Chilli: { base: 10500, volatility: 0.22, demand: 0.79, season: "Zaid", soil: "Red soil", waterNeed: "Medium" },
  Coriander: { base: 7200, volatility: 0.16, demand: 0.7, season: "Rabi", soil: "Alluvial soil", waterNeed: "Low" },
  Cumin: { base: 18500, volatility: 0.27, demand: 0.82, season: "Rabi", soil: "Sandy soil", waterNeed: "Low" },
  Fenugreek: { base: 5600, volatility: 0.14, demand: 0.66, season: "Rabi", soil: "Alluvial soil", waterNeed: "Low" },
  Brinjal: { base: 2100, volatility: 0.18, demand: 0.69, season: "Zaid", soil: "Red soil", waterNeed: "Medium" },
  Cabbage: { base: 1500, volatility: 0.16, demand: 0.66, season: "Rabi", soil: "Alluvial soil", waterNeed: "Medium" },
  Cauliflower: { base: 1800, volatility: 0.17, demand: 0.68, season: "Rabi", soil: "Alluvial soil", waterNeed: "Medium" },
  Peas: { base: 4200, volatility: 0.14, demand: 0.7, season: "Rabi", soil: "Alluvial soil", waterNeed: "Medium" },
  Spinach: { base: 1400, volatility: 0.12, demand: 0.62, season: "Rabi", soil: "Alluvial soil", waterNeed: "Medium" },
  "Okra/Bhindi": { base: 2600, volatility: 0.18, demand: 0.72, season: "Zaid", soil: "Red soil", waterNeed: "Medium" },
  "Bitter Gourd": { base: 3200, volatility: 0.19, demand: 0.68, season: "Zaid", soil: "Sandy soil", waterNeed: "Medium" },
  "Bottle Gourd": { base: 1900, volatility: 0.15, demand: 0.64, season: "Zaid", soil: "Alluvial soil", waterNeed: "Medium" },
  Pumpkin: { base: 1700, volatility: 0.13, demand: 0.61, season: "Kharif", soil: "Alluvial soil", waterNeed: "Medium" },
  Cucumber: { base: 2100, volatility: 0.17, demand: 0.69, season: "Zaid", soil: "Sandy soil", waterNeed: "Medium" },
  Carrot: { base: 2400, volatility: 0.15, demand: 0.7, season: "Rabi", soil: "Sandy soil", waterNeed: "Medium" },
  Radish: { base: 1300, volatility: 0.12, demand: 0.58, season: "Rabi", soil: "Sandy soil", waterNeed: "Medium" },
  Banana: { base: 1800, volatility: 0.15, demand: 0.75, season: "Kharif", soil: "Alluvial soil", waterNeed: "High" },
  Mango: { base: 5200, volatility: 0.21, demand: 0.84, season: "Zaid", soil: "Red soil", waterNeed: "Low" },
  Papaya: { base: 2500, volatility: 0.18, demand: 0.71, season: "Kharif", soil: "Alluvial soil", waterNeed: "Medium" },
  Guava: { base: 3600, volatility: 0.17, demand: 0.7, season: "Kharif", soil: "Alluvial soil", waterNeed: "Medium" },
  Pomegranate: { base: 7800, volatility: 0.22, demand: 0.8, season: "Zaid", soil: "Sandy soil", waterNeed: "Low" },
  Grapes: { base: 6100, volatility: 0.2, demand: 0.78, season: "Zaid", soil: "Black soil", waterNeed: "Medium" },
  Orange: { base: 4200, volatility: 0.17, demand: 0.73, season: "Rabi", soil: "Black soil", waterNeed: "Medium" },
  Lemon: { base: 3800, volatility: 0.19, demand: 0.75, season: "Zaid", soil: "Sandy soil", waterNeed: "Low" },
  Watermelon: { base: 1500, volatility: 0.2, demand: 0.76, season: "Zaid", soil: "Sandy soil", waterNeed: "Medium" },
  Muskmelon: { base: 2200, volatility: 0.19, demand: 0.69, season: "Zaid", soil: "Sandy soil", waterNeed: "Medium" },
  Coconut: { base: 2900, volatility: 0.1, demand: 0.68, season: "Kharif", soil: "Alluvial soil", waterNeed: "High" },
  Arecanut: { base: 36000, volatility: 0.18, demand: 0.72, season: "Kharif", soil: "Red soil", waterNeed: "High" },
  Jute: { base: 5100, volatility: 0.12, demand: 0.64, season: "Kharif", soil: "Alluvial soil", waterNeed: "High" },
  Flaxseed: { base: 5900, volatility: 0.13, demand: 0.61, season: "Rabi", soil: "Black soil", waterNeed: "Low" },
};

const cityMultipliers = {
  Pune: 1,
  Mumbai: 1.12,
  Nashik: 0.96,
  Nagpur: 0.92,
  Delhi: 1.08,
  Bangalore: 1.04,
  Hyderabad: 1.03,
  Ahmedabad: 1.06,
  Ludhiana: 1.02,
  Indore: 0.98,
  Jaipur: 1.01,
  Lucknow: 0.99,
  Patna: 0.95,
  Kolkata: 1.05,
  Chennai: 1.07,
  Bhopal: 0.97,
  Raipur: 0.94,
  Bhubaneswar: 0.96,
  Kochi: 1.1,
  Guwahati: 1.03,
};

const soilFit = {
  "Black soil": { Cotton: 1.16, Soybean: 1.12, Onion: 1.08, Wheat: 1.02, Grapes: 1.06, Orange: 1.07 },
  "Red soil": { Tomato: 1.13, Potato: 1.05, Rice: 0.94, Mango: 1.08, Chilli: 1.07, Turmeric: 1.08, Arecanut: 1.06 },
  "Alluvial soil": { Rice: 1.12, Wheat: 1.11, "Corn/Maize": 1.08, Mustard: 1.04, Banana: 1.07, Garlic: 1.05, Jute: 1.08 },
  "Sandy soil": { Potato: 1.14, Tomato: 1.06, Onion: 0.97, Groundnut: 1.08, Cumin: 1.08, Watermelon: 1.07 },
  "Clay soil": { Rice: 1.16, Wheat: 1.09, Cotton: 0.96, Sugarcane: 1.04 },
};

function clamp(value, min, max) {
  return Math.max(min, Math.min(max, value));
}

function textHash(text) {
  return Array.from(String(text || "")).reduce((sum, char) => sum + char.charCodeAt(0), 0);
}

const stateClimate = {
  Maharashtra: { temperature: 30, humidity: 60, rainfall: 42, condition: "Warm and partly cloudy" },
  Karnataka: { temperature: 28, humidity: 64, rainfall: 55, condition: "Mild with broken clouds" },
  Telangana: { temperature: 32, humidity: 55, rainfall: 36, condition: "Hot and dry" },
  Gujarat: { temperature: 33, humidity: 50, rainfall: 28, condition: "Dry heat" },
  Rajasthan: { temperature: 35, humidity: 38, rainfall: 16, condition: "Hot and dry" },
  Kerala: { temperature: 29, humidity: 78, rainfall: 82, condition: "Humid with showers" },
  "Tamil Nadu": { temperature: 31, humidity: 70, rainfall: 48, condition: "Humid and warm" },
  Punjab: { temperature: 29, humidity: 52, rainfall: 32, condition: "Clear and breezy" },
  Haryana: { temperature: 30, humidity: 50, rainfall: 30, condition: "Clear and dry" },
  "Uttar Pradesh": { temperature: 31, humidity: 58, rainfall: 38, condition: "Warm with haze" },
  Bihar: { temperature: 30, humidity: 66, rainfall: 54, condition: "Humid and cloudy" },
  "West Bengal": { temperature: 30, humidity: 74, rainfall: 70, condition: "Humid with thunder risk" },
  Odisha: { temperature: 31, humidity: 72, rainfall: 76, condition: "Coastal humidity" },
  Assam: { temperature: 28, humidity: 80, rainfall: 88, condition: "Heavy cloud cover" },
  Delhi: { temperature: 32, humidity: 44, rainfall: 18, condition: "Hot with haze" },
};

function seasonClimate(season) {
  if (season === "Kharif") return { temperature: 1, humidity: 8, rainfall: 28 };
  if (season === "Rabi") return { temperature: -4, humidity: -8, rainfall: -18 };
  return { temperature: 4, humidity: -2, rainfall: -8 };
}

export function getCityWeather({ state, region, season }) {
  const base = stateClimate[state] ?? { temperature: 29, humidity: 62, rainfall: 44, condition: "Mixed farm weather" };
  const seasonal = seasonClimate(season);
  const hash = textHash(`${state}-${region}`);
  const coastalBoost = ["Mumbai", "Kochi", "Chennai", "Kolkata", "Visakhapatnam", "Thiruvananthapuram", "Puri"].includes(region) ? 9 : 0;
  const plateauDryness = ["Pune", "Nashik", "Nagpur", "Bhopal", "Indore", "Jaipur"].includes(region) ? -8 : 0;
  const rainfall = Math.round(clamp(base.rainfall + seasonal.rainfall + (hash % 21) - 10 + coastalBoost + plateauDryness, 0, 150));
  const humidity = Math.round(clamp(base.humidity + seasonal.humidity + (hash % 13) - 6 + Math.round(coastalBoost / 2), 20, 95));
  const temperature = Math.round(clamp(base.temperature + seasonal.temperature + (hash % 9) - 4 - Math.round(coastalBoost / 5), 12, 44));
  const wind = Math.round(clamp(7 + (hash % 18) + rainfall / 18, 4, 32));
  const condition = rainfall > 85 ? "Heavy rain risk" : rainfall > 55 ? "Showers likely" : humidity > 74 ? "Humid and cloudy" : temperature > 34 ? "Hot and dry" : base.condition;

  return {
    temperature,
    humidity,
    rainfall,
    wind,
    condition,
    advisory: rainfall > 85
      ? "Delay spraying and check drainage before irrigation."
      : rainfall < 20
        ? "Plan irrigation and mulch to protect soil moisture."
        : "Weather is suitable for normal field operations.",
  };
}

function fallbackCropProfile(crop) {
  const hash = textHash(crop);
  return {
    base: 1800 + (hash % 90) * 95,
    volatility: 0.09 + (hash % 15) / 100,
    demand: 0.58 + (hash % 28) / 100,
    season: ["Kharif", "Rabi", "Zaid"][hash % 3],
    soil: ["Black soil", "Red soil", "Alluvial soil", "Sandy soil", "Clay soil"][hash % 5],
    waterNeed: ["Low", "Medium", "High"][hash % 3],
  };
}

export function getCropProfile(crop) {
  return crops[crop] ?? fallbackCropProfile(crop);
}

function seasonalScore(input, cropProfile) {
  if (input.season === cropProfile.season) return 1.11;
  if ((input.season === "Kharif" && input.rainfall > 80) || (input.season === "Zaid" && input.temperature > 31)) return 1.03;
  return 0.96;
}

function weatherScore(input, cropProfile) {
  const tempPenalty = Math.abs(input.temperature - 28) * 0.008;
  const humidityBoost = (input.humidity - 55) * 0.002;
  const rainfallBoost = input.rainfall > 55 ? 0.08 : input.rainfall < 18 ? -0.07 : 0.02;
  const waterPenalty = cropProfile.waterNeed === "High" && input.rainfall < 45 ? -0.08 : cropProfile.waterNeed === "Low" && input.rainfall > 95 ? -0.06 : 0;
  return clamp(1 + humidityBoost + rainfallBoost + waterPenalty - tempPenalty, 0.78, 1.2);
}

function linearRegressionEstimate(profile, features) {
  const weights = {
    base: 1,
    city: 0.29,
    soil: 0.22,
    season: 0.18,
    climate: 0.2,
    demand: 0.08,
    forecast: 0.03,
  };
  const multiplier =
    weights.base +
    (features.cityScore - 1) * weights.city +
    (features.soilScore - 1) * weights.soil +
    (features.seasonScore - 1) * weights.season +
    (features.climateScore - 1) * weights.climate +
    (profile.demand - 0.68) * weights.demand +
    features.trendLift * weights.forecast;
  return profile.base * clamp(multiplier, 0.72, 1.34);
}

function randomForestEstimate(profile, features) {
  const treeRules = [
    () => profile.base * features.cityScore * features.soilScore,
    () => profile.base * features.cityScore * features.seasonScore * features.demandTrend,
    () => profile.base * features.cityScore * features.climateScore,
    () => profile.base * features.seasonScore * features.climateScore * (1 + profile.volatility * 0.08),
    () => profile.base * features.cityScore * (features.soilScore > 1.04 ? 1.07 : 0.98) * features.demandTrend,
  ];
  const votes = treeRules.map((rule) => rule());
  return votes.reduce((sum, value) => sum + value, 0) / votes.length;
}

function lstmForecastEstimate(profile, features, input) {
  const trend = Array.from({ length: 6 }, (_, index) => {
    const wave = Math.sin((index + 1) * 0.75) * profile.volatility * 0.42;
    const growth = (index + 1) * features.trendLift * 0.2;
    return profile.base * features.cityScore * (1 + wave + growth);
  });
  const recurrentSignal = trend.reduce((state, value, index) => {
    const gate = 0.58 + index * 0.035;
    return state * gate + value * (1 - gate);
  }, profile.base * features.cityScore);
  return recurrentSignal * features.seasonScore * features.climateScore * (1 + Math.min(input.forecastDays, 10) * profile.volatility * 0.006);
}

export function predictPrice(input) {
  const profile = getCropProfile(input.crop);
  const cityScore = cityMultipliers[input.region] ?? (0.94 + (textHash(input.region) % 18) / 100);
  const soilScore = soilFit[input.soil]?.[input.crop] ?? (input.soil === profile.soil ? 1.08 : 0.98);
  const seasonScore = seasonalScore(input, profile);
  const climateScore = weatherScore(input, profile);
  const demandTrend = 1 + profile.demand * 0.09;
  const trendLift = input.forecastDays * profile.volatility * 0.014;

  const features = { cityScore, soilScore, seasonScore, climateScore, demandTrend, trendLift };
  const linearModel = linearRegressionEstimate(profile, features);
  const forestModel = randomForestEstimate(profile, features);
  const sequenceModel = lstmForecastEstimate(profile, features, input);
  const predicted = Math.round((linearModel * 0.3 + forestModel * 0.42 + sequenceModel * 0.28) / 10) * 10;
  const current = Math.round(profile.base * cityScore);
  const change = ((predicted - current) / current) * 100;
  const confidence = Math.round(clamp(88 - profile.volatility * 65 + Math.abs(soilScore - 1) * 20 + (seasonScore > 1 ? 5 : 0), 60, 95));
  const risk = change < -4 || profile.volatility > 0.2 ? "High" : change < 2 ? "Medium" : "Low";
  const demand = profile.demand > 0.77 ? "High" : profile.demand > 0.68 ? "Medium" : "Stable";

  return {
    current,
    predicted,
    change: Number(change.toFixed(1)),
    confidence,
    risk,
    demand,
    sellingWindow: change > 5 ? `Hold ${Math.min(input.forecastDays, 7)} days` : change < -2 ? "Sell soon" : "Watch market",
    cropFit: Math.round(clamp((soilScore * 0.38 + seasonScore * 0.34 + climateScore * 0.28 - 0.82) * 100, 38, 98)),
    models: [
      { name: "Linear Regression", value: Math.round(linearModel), note: "feature-weight estimate" },
      { name: "Random Forest", value: Math.round(forestModel), note: "5 rule trees" },
      { name: "LSTM Forecast", value: Math.round(sequenceModel), note: "sequence trend estimate" },
    ],
  };
}

export function buildTrend(crop, region) {
  const profile = getCropProfile(crop);
  const cityScore = cityMultipliers[region] ?? 1;
  return Array.from({ length: 12 }, (_, index) => {
    const wave = Math.sin(index * 0.8) * profile.volatility * 0.8;
    const growth = index * profile.volatility * 0.018;
    return Math.round(profile.base * cityScore * (1 + wave + growth));
  });
}

export function recommendCrops(input) {
  return Object.entries(crops)
    .map(([name, profile]) => {
      const soil = soilFit[input.soil]?.[name] ?? (input.soil === profile.soil ? 1.08 : 0.96);
      const season = seasonalScore({ ...input, crop: name }, profile);
      const water = input.rainfall > 70 && profile.waterNeed === "High" ? 1.1 : input.rainfall < 30 && profile.waterNeed === "Low" ? 1.06 : 1;
      const score = Math.round(clamp((soil * 0.42 + season * 0.34 + water * 0.24 - 0.82) * 100, 42, 96));
      return { name, score, reason: `${profile.soil}, ${profile.season} fit` };
    })
    .sort((a, b) => b.score - a.score)
    .slice(0, 5);
}

export function compareCropChoices(input, cropA, cropB) {
  const compared = [cropA, cropB].filter(Boolean).map((crop) => {
    const result = predictPrice({ ...input, crop });
    const profile = getCropProfile(crop);
    const projectedReturn = result.predicted * (Number(input.yieldQty) || 1);
    const seasonMatch = input.season === profile.season;
    const soilMatch = input.soil === profile.soil || result.cropFit >= 62;
    const waterFit =
      (profile.waterNeed === "High" && input.rainfall >= 55) ||
      (profile.waterNeed === "Medium" && input.rainfall >= 25 && input.rainfall <= 95) ||
      (profile.waterNeed === "Low" && input.rainfall <= 75);
    const costFactor = profile.waterNeed === "High" ? 0.18 : profile.waterNeed === "Medium" ? 0.14 : 0.1;
    const estimatedCost = Math.round(projectedReturn * costFactor);
    const netReturn = projectedReturn - estimatedCost;
    const score = Math.round(
      result.cropFit * 0.34 +
      result.confidence * 0.22 +
      (result.demand === "High" ? 18 : result.demand === "Medium" ? 13 : 9) +
      (result.risk === "Low" ? 16 : result.risk === "Medium" ? 9 : 3) +
      (waterFit ? 8 : 0)
    );
    return {
      crop,
      predicted: result.predicted,
      demand: result.demand,
      risk: result.risk,
      confidence: result.confidence,
      fit: result.cropFit,
      projectedReturn,
      estimatedCost,
      netReturn,
      score: clamp(score, 0, 100),
      badges: [
        seasonMatch ? "Season match" : `${profile.season} crop`,
        soilMatch ? "Soil fit" : `${profile.soil} preferred`,
        waterFit ? "Water fit" : `${profile.waterNeed} water need`,
      ],
      advice: result.risk === "High"
        ? "Keep quantity limited or wait for safer demand."
        : waterFit && soilMatch
          ? "Good candidate for current field conditions."
          : "Check soil and irrigation before choosing.",
      reason: `${profile.season} season, ${profile.soil}, ${profile.waterNeed.toLowerCase()} water need`,
    };
  }).sort((a, b) => b.score - a.score);

  return compared.map((item, index) => ({
    ...item,
    rank: index + 1,
    margin: index === 0 && compared[1] ? item.netReturn - compared[1].netReturn : item.netReturn - compared[0]?.netReturn,
  }));
}

export function diagnoseHealth(symptoms, crop = "Crop", imageMeta = null) {
  const selected = new Set(symptoms);
  const scan = imageMeta?.scan;
  const imageHint = imageMeta ? ` Image scan quality: ${scan?.quality ?? "usable"}.` : "";
  const stressRatio = scan?.stressRatio ?? 0;
  const photoConfidence = imageMeta ? Math.round(10 + Math.min(18, (scan?.leafCoverage ?? 0) * 0.18) - (scan?.blurRisk === "High" ? 8 : 0)) : 0;

  if (selected.has("White spots") && (selected.has("Yellow leaves") || selected.has("Dark lesions"))) {
    return {
      issue: `Possible fungal leaf disease in ${crop}`,
      severity: "High",
      confidence: clamp(76 + photoConfidence + stressRatio * 0.18, 60, 94),
      fertilizer: "Potassium support plus approved fungicide after expert confirmation",
      advice: "Separate affected plants if possible, remove badly affected leaves, and avoid overhead watering.",
      prevention: `Use morning irrigation, improve airflow, and avoid dense planting.${imageHint}`,
      actions: ["Isolate affected patch", "Avoid leaf wetness", "Consult agronomist", "Review in 3 days"],
    };
  }
  if (selected.has("Dry stress") || (selected.has("Dry soil") && selected.has("Slow growth"))) {
    return {
      issue: `Water stress with nutrient slowdown in ${crop}`,
      severity: "Medium",
      confidence: clamp(68 + photoConfidence + stressRatio * 0.12, 55, 90),
      fertilizer: "Balanced NPK compost mix",
      advice: "Increase watering in smaller cycles and apply compost only after soil moisture improves.",
      prevention: `Mulch soil to reduce evaporation and check drip lines for blockage.${imageHint}`,
      actions: ["Irrigate in cycles", "Mulch root zone", "Check drip flow"],
    };
  }
  if (selected.has("Yellow leaves")) {
    return {
      issue: `Nitrogen deficiency likely in ${crop}`,
      severity: "Medium",
      confidence: clamp(62 + photoConfidence + stressRatio * 0.1, 52, 88),
      fertilizer: "Nitrogen-rich organic manure",
      advice: "Apply a controlled dose after soil moisture check. Do not overapply if leaves are also spotted.",
      prevention: `Rotate crops and test soil monthly.${imageHint}`,
      actions: ["Check soil moisture", "Apply controlled dose", "Recheck new leaves"],
    };
  }
  return {
    issue: `No critical issue detected for ${crop}`,
    severity: "Low",
    confidence: clamp(58 + photoConfidence, 50, 84),
    fertilizer: "Maintain regular compost schedule",
    advice: "Monitor leaves and soil moisture weekly.",
    prevention: `Keep field sanitation and balanced irrigation.${imageHint}`,
    actions: ["Monitor weekly", "Keep field clean", "Avoid overwatering"],
  };
}
